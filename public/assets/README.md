# Brand assets

Drop your real design files here, then tell me and I'll wire them into the
components (replacing the code-drawn placeholders).

## What I need (in priority order)

1. **Hero "sound sun" graphic** — the concentric rainbow sound-rings.
   - Best formats: transparent PNG (2x), SVG, Lottie JSON, or MP4/WebM if it's
     an actual video animation on the original.
   - Suggested filename: `hero-rings.*`
   - Currently faked by `components/SoundRings.tsx`.

2. **Logo / wordmark** — if "SOUND BATH" is a custom typeface or a logo image
   (not just a font). SVG preferred. Suggested: `wordmark.svg`.
   - Currently rendered as text in the Cormorant Garamond serif font.

3. **Side wave lines** — the purple flowing lines on the hero edges, if you
   have them as art. SVG preferred. Suggested: `side-waves.svg`.
   - Currently faked by `components/SideWaves.tsx`.

4. **Favicon** — `favicon.ico` (or PNG) for the browser tab.

5. **Platform links** — the real URLs for Bandcamp, Amazon, Apple, Spotify,
   YouTube, Instagram, and the Connect/Catalog destinations. (Just paste them in
   chat — no files needed.)

## Fonts

If "SOUND BATH" or the body text uses a specific licensed font, send the font
files (woff2) or the exact Google Fonts name and I'll swap it in.
